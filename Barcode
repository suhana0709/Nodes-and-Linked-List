def itemPrice(barcode):
    #list to append ASCII codes
    li = []
    for i in barcode:
        n = ord(i)   # Ordi: built in func to get the unicode
        print("unicode: ",n)
        #finding the maximum digit of the ASCII codes
        if n//10:
            maxi = 0
            while n>0:
                if n%10 > maxi:
                    maxi = n%10
                n = n//10
            li.append(maxi)
            print(li)
        else:
            li.append(n)
            print(li)
            
    #returning the sum of the list
    return sum(li)    
    
#driver code
barcode = input("Enter the barcode: ")
print(itemPrice(barcode))
